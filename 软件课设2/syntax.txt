<S> <S> <S>
<S> <include语句>
<S> <using语句> ;
<S> <赋值语句组> ;
<S> <空语句> ;
<S> <函数块> 
<S> <while块>
<S> <if块>

<include语句> # include " 标识符 "

<using语句> using namespace 标识符

<赋值语句组> <赋值语句组> , <赋值语句>
<赋值语句组>  <赋值语句>
<赋值语句> 标识符
<赋值语句> 标识符 = <赋值语句>
<赋值语句> 标识符 = <表达式>
<表达式> <表达式> op <表达式>
<表达式> ( <表达式> )
<表达式> <标常>
<标常> 常数
<标常> 标识符

<空语句> 

<函数块> <类型> 标识符 ( <形参表> ) { <函数内语句> }
<形参表> 
<形参表> <形参> , <形参表>
<形参> <类型> 标识符
<函数内语句> <S>
<函数内语句> <函数内语句>
<函数内语句> return <标常> ;

<while块> while ( <表达式> ) { <while块内语句> }
<while块内语句> <while块内语句> <while块内语句>
<while块内语句> <S>
<while块内语句> break ;
<while块内语句> continue ;

<if块> if ( <表达式> ) { <S> }

